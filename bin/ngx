#!/usr/bin/env node

"use strict";

var path = require('path'),
  real = require('fs').realpathSync,
  dirname = require('path').dirname,
  join = require('path').join;

var path = real(__filename);

var cli = join(dirname(path), '..', 'src', 'cli');

require(cli).run();



